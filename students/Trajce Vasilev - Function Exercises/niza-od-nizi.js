const inputs = [
    ['a', 'b', 'c'],
    ['c', 'd', 'f'],
    ['d', 'f', 'g', 'h'],
    ['h', 'i', 'j', 'k', 'l']
]

inputs.forEach((array) => {
    console.log(array)
    console.log(array.length)
})
